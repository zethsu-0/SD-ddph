<Window x:Class="ddph.OnlineOrders"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Online Orders"
        Height="760"
        Width="1320"
        MinHeight="640"
        MinWidth="1100"
        Background="{StaticResource ThemeBaseBrush}"
        WindowState="Maximized"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Close Button Overlay -->
        <Button Width="100"
                Height="36"
                Margin="0,-18,-18,0"
                HorizontalAlignment="Right"
                VerticalAlignment="Top"
                Panel.ZIndex="100"
                FontSize="18"
                FontWeight="SemiBold"
                Background="{StaticResource ThemeAccentBrush}"
                Foreground="{StaticResource ThemeInkBrush}"
                BorderBrush="{StaticResource ThemeBorderBrush}"
                BorderThickness="1"
                ToolTip="Back"
                Click="CloseButton_Click">
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="10">
                        <TextBlock Text="&#x2190;"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"
                                   FontSize="18"
                                   FontWeight="SemiBold"
                                   Foreground="{TemplateBinding Foreground}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Opacity" Value="0.82"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="Opacity" Value="0.65"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Button.Template>
        </Button>

        <Border Background="{StaticResource ThemeBaseBrush}"
                BorderThickness="0"
                BorderBrush="Transparent"
                Margin="-18,-18,-18,0"
                Padding="18,14">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel>
                    <TextBlock Text="Online Orders"
                               FontSize="24"
                               FontWeight="SemiBold"
                               Foreground="{StaticResource ThemeInkBrush}"/>
                    <TextBlock Margin="0,6,0,0"
                               Text="Scan live website orders, review details, and update their status."
                               Foreground="{StaticResource ThemeInkSoftBrush}"/>
                </StackPanel>

                <TextBox
                         Width="280"
                         Height="38"
                         VerticalContentAlignment="Center"
                         Padding="12,0"
                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Grid.ColumnSpan="2" Margin="816,8,76,8"/>

                <Button
                        Width="90"
                        Height="38"
                        Content="Refresh"
                        Command="{Binding RefreshCommand}" Margin="1146,8,26,8"/>


            </Grid>
        </Border>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="380"/>
                <ColumnDefinition Width="18"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Background="{StaticResource ThemeSurfaceBrush}"
                    BorderBrush="{StaticResource ThemeBorderBrush}"
                    BorderThickness="1"
                    CornerRadius="14"
                    Padding="12">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="12"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="Incoming Orders"
                               FontSize="18"
                               FontWeight="SemiBold"
                               Foreground="{StaticResource ThemeOnSurfaceBrush}"/>

                    <ListBox Grid.Row="2"
                             ItemsSource="{Binding FilteredOrders}"
                             SelectedItem="{Binding SelectedOrder, Mode=TwoWay}"
                             BorderThickness="0"
                             Background="Transparent"
                             ScrollViewer.VerticalScrollBarVisibility="Auto">
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="Padding" Value="0"/>
                                <Setter Property="Margin" Value="0,0,0,12"/>
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            </Style>
                        </ListBox.ItemContainerStyle>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border Padding="14"
                                        Background="{StaticResource ThemeBaseBrush}"
                                        BorderBrush="{StaticResource ThemeBorderBrush}"
                                        BorderThickness="1"
                                        CornerRadius="12">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <StackPanel>
                                            <TextBlock Text="{Binding DisplayName}"
                                                       FontSize="17"
                                                       FontWeight="SemiBold"
                                                       Foreground="{StaticResource ThemeInkBrush}"/>
                                            <TextBlock Margin="0,5,0,0"
                                                       Text="{Binding CustomerPhone}"
                                                       Foreground="{StaticResource ThemeInkSoftBrush}"/>
                                            <TextBlock Margin="0,3,0,0"
                                                       Text="{Binding CustomerEmail}"
                                                       Foreground="{StaticResource ThemeInkSoftBrush}"
                                                       TextTrimming="CharacterEllipsis"/>
                                            <TextBlock Margin="0,8,0,0"
                                                       Text="{Binding PickupDate, StringFormat=Pickup: {0}}"
                                                       Foreground="{StaticResource ThemeInkBrush}"
                                                       FontWeight="SemiBold"/>
                                        </StackPanel>

                                        <StackPanel Grid.Column="1"
                                                    HorizontalAlignment="Right">
                                            <Border Padding="10,5"
                                                    Background="{StaticResource ThemeBaseBrush}"
                                                    CornerRadius="12">
                                                <TextBlock Text="{Binding Status}"
                                                           Foreground="{StaticResource ThemeInkBrush}"
                                                           FontWeight="Bold"/>
                                            </Border>
                                            <TextBlock Margin="0,12,0,0"
                                                       HorizontalAlignment="Right"
                                                       Text="{Binding Total, StringFormat=₱{0:F2}}"
                                                       FontWeight="Bold"
                                                       Foreground="{StaticResource ThemeOnSurfaceBrush}"/>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Border>

            <Border Grid.Column="2"
                    Background="{StaticResource ThemeSurfaceBrush}"
                    BorderBrush="{StaticResource ThemeBorderBrush}"
                    BorderThickness="1"
                    CornerRadius="14"
                    Padding="16">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Text="Order Details"
                                       FontSize="22"
                                       FontWeight="SemiBold"
                                       Foreground="{StaticResource ThemeOnSurfaceBrush}"/>
                        </Grid>

                        <Border Margin="0,16,0,0"
                                Padding="18"
                                Background="#f0e8de"
                                BorderBrush="#c5b09a"
                                BorderThickness="1"
                                CornerRadius="14">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="14"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <Border Width="62"
                                        Height="62"
                                        Background="#4b607f"
                                        CornerRadius="31">
                                    <TextBlock Text="👤"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center"
                                               FontSize="28"/>
                                </Border>

                                <StackPanel Grid.Column="2">
                                    <TextBlock Text="{Binding SelectedOrder.DisplayName}"
                                               FontSize="20"
                                               FontWeight="Bold"
                                               Foreground="#2c1f12"/>
                                    <TextBlock Margin="0,8,0,0"
                                               Text="{Binding SelectedOrder.CustomerPhone}"
                                               Foreground="#8a7060"/>
                                    <TextBlock Margin="0,4,0,0"
                                               Text="{Binding SelectedOrder.CustomerEmail}"
                                               Foreground="#8a7060"/>
                                </StackPanel>
                            </Grid>
                        </Border>

                        <Border Margin="0,16,0,0"
                                Padding="18"
                                Background="#f4ece2"
                                BorderBrush="#E6DDD1"
                                BorderThickness="1"
                                CornerRadius="14">
                            <StackPanel>
                                <TextBlock Text="Order Summary"
                                           FontSize="18"
                                           FontWeight="SemiBold"
                                           Foreground="#4b607f"/>

                                <ItemsControl Margin="0,16,0,0"
                                              ItemsSource="{Binding SelectedOrder.Items}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Grid Margin="0,0,0,14">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="14"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>

                                                <Border Width="54"
                                                        Height="54"
                                                        Background="#F6EFE5"
                                                        BorderBrush="#E6DDD1"
                                                        BorderThickness="1"
                                                        CornerRadius="10">
                                                    <TextBlock Text="🧁"
                                                               HorizontalAlignment="Center"
                                                               VerticalAlignment="Center"
                                                               FontSize="24"/>
                                                </Border>

                                                <StackPanel Grid.Column="2"
                                                            VerticalAlignment="Center">
                                                    <TextBlock Text="{Binding Name}"
                                                               FontSize="16"
                                                               FontWeight="SemiBold"
                                                               Foreground="#2c1f12"/>
                                                    <TextBlock Margin="0,4,0,0"
                                                               Text="{Binding Quantity, StringFormat=Qty: {0}}"
                                                               Foreground="#8a7060"/>
                                                </StackPanel>

                                                <TextBlock Grid.Column="3"
                                                           VerticalAlignment="Center"
                                                           Text="{Binding Subtotal, StringFormat=₱{0:F0}}"
                                                           FontWeight="Bold"
                                                           Foreground="#4b607f"/>
                                            </Grid>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>

                                <Separator Margin="0,8,0,12"/>

                                <TextBlock HorizontalAlignment="Right"
                                           Text="{Binding SelectedOrder.Total, StringFormat=Total: ₱{0:F2}}"
                                           FontSize="24"
                                           FontWeight="Bold"
                                           Foreground="#4b607f"/>
                            </StackPanel>
                        </Border>

                        <TextBlock Margin="0,18,0,10"
                                   Text="Quick Actions"
                                   FontSize="18"
                                   FontWeight="SemiBold"
                                   Foreground="#2c1f12"/>

                        <UniformGrid Columns="2"
                                     Rows="2">
                            <Button Margin="0,0,10,10"
                                    Height="42"
                                    Command="{Binding ConfirmOrderCommand}"
                                    Background="#DDF2E0"
                                    BorderBrush="#4CAF68"
                                    Foreground="#1C6A34"
                                    Content="Confirm Order"/>
                            <Button Margin="0,0,0,10"
                                    Height="42"
                                    Command="{Binding NeedsAdjustmentCommand}"
                                    Background="#FFF0D8"
                                    BorderBrush="#F2A43C"
                                    Foreground="#A85D00"
                                    Content="Needs Adjustment"/>
                            <Button Margin="0,0,10,0"
                                    Height="42"
                                    Command="{Binding MarkCompleteCommand}"
                                    Background="#d8e2ef"
                                    BorderBrush="#4b607f"
                                    Foreground="#4b607f"
                                    Content="Mark Complete"/>
                            <Button Height="42"
                                    Command="{Binding CancelOrderCommand}"
                                    Background="#FFE1E1"
                                    BorderBrush="#E36464"
                                    Foreground="#A63131"
                                    Content="Cancel Order"/>
                        </UniformGrid>

                        <Border Margin="0,18,0,0"
                                Padding="18"
                                Background="#f0e8de"
                                BorderBrush="#c5b09a"
                                BorderThickness="1"
                                CornerRadius="14">
                            <StackPanel>
                                <TextBlock Text="Current Status"
                                           FontSize="16"
                                           FontWeight="SemiBold"
                                           Foreground="#2c1f12"/>

                                <Border Margin="0,12,0,0"
                                        Padding="12"
                                        BorderBrush="#E6DDD1"
                                        BorderThickness="1"
                                        CornerRadius="10"
                                        Background="#f4ece2">
                                    <TextBlock Text="{Binding SelectedOrder.Status}"
                                               FontWeight="Bold"
                                               Foreground="#B44343"/>
                                </Border>

                                <TextBlock Margin="0,18,0,0"
                                           Text="Change Status"
                                           FontSize="16"
                                           FontWeight="SemiBold"
                                           Foreground="#2c1f12"/>

                                <ComboBox Margin="0,10,0,0"
                                          Height="38"
                                          ItemsSource="{Binding StatusOptions}"
                                          SelectedItem="{Binding SelectedStatus, UpdateSourceTrigger=PropertyChanged}"/>

                                <Button Margin="0,12,0,0"
                                        Height="40"
                                        HorizontalAlignment="Left"
                                        Width="120"
                                        Command="{Binding SaveStatusCommand}"
                                        Content="Save Status"/>

                                <TextBlock Margin="0,18,0,0"
                                           Text="Internal Notes"
                                           FontSize="16"
                                           FontWeight="SemiBold"
                                           Foreground="#20354F"/>

                                <TextBox Margin="0,10,0,0"
                                         MinHeight="90"
                                         Text="{Binding SelectedOrder.Notes}"
                                         TextWrapping="Wrap"
                                         AcceptsReturn="True"
                                         IsReadOnly="True"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>
    </Grid>
</Window>
